package net.abrikoos.blockout.chunkgenerators;

// Compilation stub — real implementation (extending NoiseChunkGenerator) lives in the Fabric module.
// This stub keeps common-module code compilable without pulling Yarn-named MC classes into the
// namedElements jar, which would crash the Architectury Runtime Transformer in NeoForge's
// Mojang-mapped classpath environment.
public abstract class BlockoutNoiseGenerator {
}
