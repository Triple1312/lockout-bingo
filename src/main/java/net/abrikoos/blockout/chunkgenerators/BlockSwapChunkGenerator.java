package net.abrikoos.blockout.chunkgenerators;

import net.minecraft.block.Block;

import java.util.Map;

// Compilation stub — real implementation lives in the Fabric module.
// ChunkRegionMixin references getSwapMap(); this stub satisfies the compiler.
public class BlockSwapChunkGenerator extends BlockoutNoiseGenerator {
    public Map<Block, Block> getSwapMap() { return null; }
}
