package net.abrikoos.blockout.chunkgenerators;

import java.util.Map;

// NeoForge stub. ChunkRegionMixin references this type via instanceof; it is never instantiated
// on NeoForge so instanceof always returns false and the mixin early-returns harmlessly.
public class BlockSwapChunkGenerator {
    public Map<?, ?> getSwapMap() { return null; }
}
